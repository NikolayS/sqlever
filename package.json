{
  "name": "sqevo",
  "version": "0.1.0",
  "description": "Sqitch-compatible PostgreSQL migration tool",
  "type": "module",
  "bin": {
    "sqevo": "./src/cli.ts"
  },
  "scripts": {
    "build": "bun build src/cli.ts --compile --outfile dist/sqevo",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "dependencies": {
    "pg": "^8.11.0"
  }
}
